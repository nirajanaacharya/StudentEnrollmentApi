using Microsoft.AspNetCore.Identity;

namespace StudentEnrollmentApi.Models
{
    public class User : IdentityUser
    {
        // additional properties are not needed , identity provide the built in 
    }
}